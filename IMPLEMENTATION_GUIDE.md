# МВП-Аналитика: Руководство по реализации

## 📋 Статус реализации (Фаза I завершена ✅)

### Фаза I: Переподготовка БД и моделей (ЗАВЕРШЕНО ✅)

#### 1. SQL-схемы созданы ✅
- **01_schema_municipalities.sql** - Справочники (МО, периоды, показатели, штрафы, методика)
- **02_schema_facts.sql** - Таблицы фактов (значения показателей, штрафы, рейтинги)
- **03_seed_indicators_and_scales.sql** - Инициализация шкал и справочников по методике v1
- **04_seed_test_data.sql** - Тестовые данные для 3 зон (зелёная, жёлтая, красная)

#### 2. Backend структура ✅
- **services/calc-scoring.js** - Расчётный движок для вычисления баллов
- **routes/map.js** - API для карты (хлороплета)
- **routes/mo-card.js** - API для карточки МО
- **routes/rating.js** - API для таблицы рейтинга
- **db/postgres.js** - Модуль подключения к БД
- **server-new.js** - Главный сервер (переименовать в server.js)

#### 3. Конфигурация ✅
- **package.json** - Обновлён для нового проекта (Node.js 18+)
- **.env.example** - Требуется создать

---

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+
- PostgreSQL 14+
- npm или yarn

### 1. Установка зависимостей

```bash
npm install
```

### 2. Подготовка БД

```bash
# Создать .env файл (см. .env.example)
cp .env.example .env

# Отредактировать DATABASE_URL в .env
# Пример: postgresql://user:password@localhost:5432/mvp_analitika

# Инициализировать БД (создать таблицы)
npm run db:init

# Загрузить тестовые данные
npm run db:seed

# Сбросить БД (если нужно начать заново)
npm run db:reset
```

### 3. Запуск сервера

```bash
# Развитие (с автоперезагрузкой)
npm run dev

# Продакшн
npm start
```

Сервер будет доступен на **http://localhost:3000**

---

## 📊 API Endpoints (Фаза I)

### 1. Карта (Хлороплета)

**GET /api/map**
```
Query параметры:
- period=202406 (формат YYYYMM)
- version=v1 (версия методики)
- colorBy=total_score|completion (критерий окраски)

Ответ: Все МО с баллами, координатами, легендой и статистикой
```

Примеры:
```bash
curl "http://localhost:3000/api/map?period=202406&colorBy=total_score"
curl "http://localhost:3000/api/map/top-bottom?period=202406&limit=5"
curl "http://localhost:3000/api/map/stats?period=202406"
```

### 2. Карточка МО

**GET /api/mo/:mo_id**
```
Query параметры:
- period=202406
- version=v1
- view=total|public|closed (какие показатели показать)

Ответ: Полный профиль МО с показателями, штрафами, динамикой, рекомендациями
```

Примеры:
```bash
curl "http://localhost:3000/api/mo/1?period=202406&view=total"
```

### 3. Таблица рейтинга

**GET /api/rating**
```
Query параметры:
- period=202406
- version=v1
- sort=score_total|score_public|zone (поле сортировки)
- order=DESC|ASC
- zone=green|yellow|red (фильтр по зоне)
- page=1
- limit=20 (макс 100)

Ответ: Список МО с рейтингами и пагинацией
```

**GET /api/rating/compare**
```
Query параметры:
- period=202406
- mo_ids=1,2,3 (список ID для сравнения, макс 5)

Ответ: Сравнительная таблица по всем показателям
```

**GET /api/rating/search**
```
Query параметры:
- q=липецк (поисковая строка)
- period=202406

Ответ: Результаты поиска МО
```

Примеры:
```bash
curl "http://localhost:3000/api/rating?sort=score_total&zone=green"
curl "http://localhost:3000/api/rating/compare?mo_ids=1,2,4"
curl "http://localhost:3000/api/rating/search?q=елец"
```

---

## 📁 Структура проекта

```
MVP-analitika/
├── sql/                      # SQL миграции и данные
│   ├── 01_schema_municipalities.sql
│   ├── 02_schema_facts.sql
│   ├── 03_seed_indicators_and_scales.sql
│   └── 04_seed_test_data.sql
├── db/
│   └── postgres.js           # Подключение к БД
├── services/
│   └── calc-scoring.js       # Расчётный движок
├── routes/
│   ├── map.js                # API карты
│   ├── mo-card.js            # API карточки МО
│   └── rating.js             # API рейтинга
├── public/                   # Статические файлы (фронтенд)
│   ├── index.html
│   ├── css/
│   ├── js/
│   └── assets/
├── package.json
├── server-new.js             # Главный сервер (переименовать в server.js)
├── .env.example              # Шаблон переменных окружения
└── README.md                 # Этот файл
```

---

## 🗄️ Модель данных

### Справочники (Dimensions)
- **dim_municipalities** - 10 МО Липецкой области
- **dim_period** - Периоды отчетности (месячные, полугодовые, годовые)
- **dim_indicators** - 17 критериев (9 публичных + 8 закрытых)
- **dim_penalties** - 3 штрафных критерия
- **dim_methodology_versions** - Версионирование методики
- **map_scale** - Шкалы для расчета баллов
- **src_registry** - Реестр источников данных

### Факты (Facts)
- **fact_indicator_values** - Значения показателей по МО и периодам
- **fact_penalties** - Штрафные события
- **fact_summary_ratings** - Итоговые рейтинги
- **fact_events** - Журнал событий

### Служебные
- **audit_log** - Журнал действий
- **load_status** - Статус загрузки данных
- **calculation_history** - История расчетов

---

## 📊 Методика оценки (v1)

### Публичный рейтинг (макс 31)
1. Поддержка руководства (0-3)
2. Выполнение задач АГП (0-5)
3. Позиционирование главы (0-3)
4. Проектная деятельность (0-3)
5. Молодёжь в добровольчестве (0-3)
6. Активные в Движении Первых 6-18 (0-3)
7. Вовлечённость в работу с ветеранами (0-3)
8. Кадровый резерв (0-3)
9. Эффективность грантов (0-3)

### Закрытый рейтинг (макс 35)
1. Партийное мнение (0-6)
2. Альтернативное мнение (0/2/4)
3. АГП уровень (0/3/5)
4. АГП качество (0/3/5)
5. Экономическая привлекательность (1/2/3)
6. Активность ветеранов (0-6)
7. Проект "Гордость Липецкой земли" (0/2)

### Штрафы (отрицательные)
1. Конфликты с рег. властью (0/-2/-3)
2. Внутримуниципальные конфликты (-1/-2/-3)
3. ПО и репутационные потери (0/-2/-5)

### Зоны
- 🟢 **Зелёная**: 53-66 баллов (эффективная)
- 🟡 **Жёлтая**: 29-52 балла (средняя)
- 🔴 **Красная**: 0-28 баллов (требует улучшения)

---

## ✅ Критерии приёмки Фазы I

- ✅ SQL-схема полностью создана и инициализирована
- ✅ Тестовые данные загружены (3 профиля МО по зонам)
- ✅ Расчётный двигок работает корректно
- ✅ API маршруты возвращают корректные данные
- ✅ Баллы соответствуют методике
- ✅ Динамика рассчитывается (MoM/YoY)
- ✅ Дорожная карта улучшений генерируется

---

## 🔧 Далее: Фаза II (2-3 недели)

1. ✏️ **ETL для загрузки данных** - Разбор Excel шаблонов, валидация, расчет баллов
2. 🔐 **RBAC (маскирование метрик)** - Контроль доступа к закрытым показателям
3. 🗺️ **Фронтенд (карта, карточка, рейтинг)** - HTML/CSS/JS для интерфейса
4. 📤 **Экспорт (PDF/Excel)** - Выгрузка профилей и отчётов
5. 🧪 **Тесты и приёмка** - Юнит-тесты, интеграционные тесты

---

## 🐛 Troubleshooting

### "PostgreSQL connection failed"
- Проверьте переменную `DATABASE_URL` в .env
- Убедитесь, что PostgreSQL запущена
- Проверьте доступ к БД: `psql $DATABASE_URL`

### "Tables not found"
- Выполните: `npm run db:init && npm run db:seed`
- Проверьте логи инициализации

### "Какой-то город не отображается на карте"
- Проверьте таблицу `dim_municipalities`
- Убедитесь, что `is_active = TRUE`
- Проверьте наличие координат (lat, lon)

---

## 📞 Контакты и поддержка

Проблемы и предложения: https://github.com/Aleksey341/MVP-analitika/issues

---

**Версия документации**: 1.0
**Дата обновления**: 2024-11-04
**Статус**: Фаза I Завершена ✅
