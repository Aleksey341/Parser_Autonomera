# Полная инструкция установки парсера АВТОНОМЕРА777

## Шаг 1: Установка Node.js

### На Windows:

1. **Скачайте Node.js** с официального сайта:
   - Перейдите на https://nodejs.org/
   - Нажмите на зеленую кнопку "LTS" (рекомендуется)
   - Скачайте установщик (.msi файл)

2. **Установите Node.js:**
   - Запустите скачанный файл
   - Нажимайте "Next" на всех экранах
   - Убедитесь, что отмечены опции:
     ✓ Node.js runtime
     ✓ npm package manager
   - Нажмите "Install"
   - Дождитесь завершения установки
   - Нажмите "Finish"

3. **Проверьте установку:**
   - Откройте "Командную строку" (cmd.exe)
   - Введите: `node --version`
   - Должна появиться версия, например: v18.17.0
   - Введите: `npm --version`
   - Должна появиться версия npm, например: 9.6.7

## Шаг 2: Подготовка проекта

1. **Откройте Командную строку:**
   - Нажмите Windows + R
   - Введите: `cmd`
   - Нажмите Enter

2. **Перейдите в папку проекта:**
   ```
   cd C:\Users\cobra
   ```

3. **Проверьте наличие файлов:**
   ```
   dir
   ```
   Вы должны увидеть:
   - `parser.js`
   - `server.js`
   - `package.json`
   - `README.md`
   - папка `public/` (с index.html)

## Шаг 3: Установка зависимостей

В Командной строке выполните:

```
npm install
```

Процесс:
- Потребует несколько минут
- Будут скачаны все необходимые пакеты
- Создастся папка `node_modules/`
- Появится сообщение: `added X packages in Ys`

Если что-то пошло не так:
```
npm install --legacy-peer-deps
```

## Шаг 4: Запуск сервера

### Способ 1: Через батник (самый легкий)

1. В папке `C:\Users\cobra` найдите файл `run.bat`
2. Дважды кликните на него
3. Должно открыться окно с текстом "Запуск сервера..."
4. Откройте браузер и перейдите на: http://localhost:3000

### Способ 2: Через Командную строку

1. Откройте Командную строку
2. Перейдите в папку: `cd C:\Users\cobra`
3. Введите: `node server.js`
4. Должны увидеть:
   ```
   🚀 API сервер запущен на http://localhost:3000
   ```
5. Откройте браузер и перейдите на: http://localhost:3000

## Шаг 5: Использование парсера

### Через веб-интерфейс:

1. **Установите параметры:**
   - Минимальная цена: 0 (или ваше значение)
   - Максимальная цена: 10000000 (или ваше значение)
   - Регион: оставьте пусто для всех регионов
   - Максимум страниц: 50
   - Задержка: 1000 мс

2. **Нажмите "Начать парсинг"**

3. **Ждите завершения:**
   - Видите прогресс-бар
   - Количество загруженных объявлений растет
   - Статус: "running" → "completed"

4. **Просмотрите результаты:**
   - Вкладка "Обзор" - общая статистика
   - Вкладка "Данные" - все объявления в таблице
   - Вкладка "По регионам" - распределение по регионам

5. **Экспортируйте данные:**
   - Нажмите "📥 Экспортировать"
   - Скачается CSV файл с датой

## Устранение проблем

### Проблема: "Node.js не установлен"
**Решение:**
- Скачайте Node.js с https://nodejs.org/
- Переустановите его
- Перезагрузитесь
- Проверьте: `node --version`

### Проблема: "npm install выдает ошибку"
**Решение:**
```
npm cache clean --force
npm install
```

### Проблема: "Порт 3000 занят"
**Решение:**
1. Откройте `server.js` в текстовом редакторе
2. Найдите строку: `const PORT = process.env.PORT || 3000;`
3. Измените 3000 на другой порт, например 3001
4. Сохраните файл
5. Перезапустите сервер

### Проблема: "Сервер не подключается в браузере"
**Решение:**
1. Убедитесь, что сервер запущен (видите "🚀 API сервер запущен")
2. Откройте браузер (Chrome, Firefox, Edge)
3. Попробуйте перезагрузить страницу (F5)
4. Проверьте правильность URL: http://localhost:3000

### Проблема: "Парсинг не начинается"
**Решение:**
1. Убедитесь, что сервер запущен
2. Проверьте интернет-соединение
3. Попробуйте уменьшить "Максимум страниц" (например, на 5)
4. Посмотрите логи в консоли сервера

## Команды для разных операций

### Запустить только парсер (без веб-интерфейса):
```
node parser.js
```
Результаты сохранятся в файлы.

### Запустить в режиме разработки (быстрее для тестирования):
```
node parser.js --dev
```

### Запустить сервер и парсер:
```
node server.js
```

### Остановить сервер:
Нажмите Ctrl+C в Командной строке

## Файловая структура после установки

```
C:\Users\cobra\
├── parser.js              ← Основной парсер
├── server.js              ← REST API сервер
├── package.json           ← Зависимости
├── run.bat                ← Батник для запуска
├── README.md              ← Документация
├── INSTALL.md             ← Эта инструкция
├── EXAMPLES.md            ← Примеры использования
├── public/
│   └── index.html         ← Веб-интерфейс
├── node_modules/          ← Установленные пакеты
│   ├── axios/
│   ├── cheerio/
│   ├── express/
│   └── ...
└── autonomera777_*.csv    ← Результаты парсинга
    └── autonomera777_*.json
```

## Типичный рабочий процесс

1. **Запуск:**
   ```
   run.bat
   ```
   или
   ```
   node server.js
   ```

2. **Откройте браузер:**
   ```
   http://localhost:3000
   ```

3. **Установите параметры** и нажмите "Начать парсинг"

4. **Дождитесь завершения** (смотрите прогресс в таблице)

5. **Экспортируйте результаты** (нажмите кнопку "Экспортировать")

6. **Откройте CSV файл** в Excel или Google Sheets

## Требования к системе

- **ОС:** Windows 10 или выше
- **RAM:** минимум 512 MB (рекомендуется 2 GB)
- **Диск:** 500 MB свободного места
- **Интернет:** постоянное соединение
- **Браузер:** Chrome, Firefox, Edge или Safari (последние версии)

## Безопасность

- Парсер работает **локально** на вашем компьютере
- Данные **не отправляются** никому
- API доступен только на `localhost`
- Все данные остаются у вас

## Что дальше?

- Прочитайте `README.md` для подробной документации
- Смотрите `EXAMPLES.md` для примеров использования
- Используйте веб-интерфейс для удобного парсинга
- Экспортируйте результаты в CSV или JSON

## Техническая поддержка

Если возникли проблемы:
1. Проверьте все логи в консоли
2. Убедитесь, что все файлы скопированы корректно
3. Переустановите зависимости: `npm install`
4. Перезагрузитесь
5. Попробуйте еще раз

---

## Быстрый старт (для нетерпеливых):

```
1. Установите Node.js с https://nodejs.org/
2. Откройте Командную строку: Windows + R → cmd
3. Перейдите в папку: cd C:\Users\cobra
4. Установите пакеты: npm install
5. Запустите батник: run.bat
6. Откройте браузер: http://localhost:3000
7. Нажмите "Начать парсинг"
8. Дождитесь завершения
9. Нажмите "Экспортировать"
```

**Все готово! Удачи! 🚀**
